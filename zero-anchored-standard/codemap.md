# zero-anchored-standard/

## Responsibility

Retains the repository's earlier comparison preset: a synthetic first user
anchor followed by zero-tool bootstrap and later Standard exposure.

## Boundary

This directory is not mounted by the default `preset/` composition and is kept
for historical experiments and regression tests. New runtime work belongs in
`preset/` and follows `docs/plan.md`.
